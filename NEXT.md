# Next (Ghost Continuum)

Low-risk leftover for the next COOK tick:

1. Vendor Three.js r160 at `packages/hub-ui/public/vendor/three.module.js` so the holographic map works air-gapped (ROADMAP marks the path; the file is still a README-only drop). Do not bump the version.

Hold (not a COOK leftover): hardened eBPF probes, K8s operator, hosted plugin registry, production Tauri builds.
