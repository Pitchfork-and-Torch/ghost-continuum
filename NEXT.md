# Next (Ghost Continuum)

Low-risk leftover for the next COOK tick:

1. Align `scripts/post-deploy-seo.ps1` copy check to Crystal Seal (Unix already does; PowerShell still looks for Crystal Membrane and can false-miss). Do not bump the version.

Hold (not a COOK leftover): hardened eBPF probes, K8s operator, hosted plugin registry, production Tauri builds.
