# Next (Ghost Continuum)

Low-risk leftover for the next COOK tick:

1. Hub security headers on Command Nexus responses (`X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, framing-only CSP, `Referrer-Policy: no-referrer`). Do not bump the version.

Hold (not a COOK leftover): hardened eBPF probes, K8s operator, hosted plugin registry, production Tauri builds.
