# Living Deception Continuum — Roadmap

## Phase 1 — Genome Heart + Trust Foundation ✅ v0.3.0

- Deception Genome schema, GA evolution, fitness scoring, runtime morph fragments
- Ghost LAN genome integration, Merkle ledger, Continuum nexus, hub API

## Phase 2 — Cognitive Depth + Phantom Mesh ✅ v1.0.0

- [x] Admin shell conversation loop (`packages/narrative/src/shell.js`)
- [x] Echo Reality document generator (logs, emails, configs)
- [x] Phantom Persona cross-session shell memory
- [x] Phantom Mesh file-based federated strategy gossip
- [x] NL query interface (`/api/continuum/query`)
- [x] Enhanced efficacy dashboard + heatmaps in Command Nexus

## Phase 3 — Deep Veil + Mirage Core ✅ v1.0.0

- [x] Deep Veil Linux eBPF script placeholder + `/proc/net/tcp` fallback
- [x] Mirage Core Docker/Podman decoy spawn/teardown
- [x] Fitness-triggered mirage spawn (`packages/continuum/src/triggers.js`)
- [x] Anti-analysis morph in Ghost LAN sentinel

## Phase 4 — Immersive Command Nexus ✅ v1.0.0

- [x] Three.js holographic intrusion map (CDN, opt-in WebGL)
- [x] Forensic Time Machine branching replay (`/api/continuum/replay`)
- [x] Ghost CLI animations (`bin/ghost-cli.js`)
- [x] Tauri desktop scaffold (`apps/nexus-desktop/`)

## Phase 5 — Production Fabric ✅ v1.0.0

- [x] Helm chart (`deploy/helm/ghost-continuum/`)
- [x] systemd + launchd + Windows service scripts
- [x] STIX/TAXII export (`/api/continuum/taxii`, `/taxii2/`)
- [x] Plugin loader + marketplace conventions (`packages/plugins/`)
- [x] Threat model (`docs/THREAT-MODEL.md`)

---

**v1.0.0** ships the full continuum.

## Phase 6 — Home operator polish (in progress)

- [x] Printable HTML weekly home report (browser Print → PDF) alongside markdown/JSON  
- [x] Offline Three.js vendor path (`public/vendor/three.module.js` preferred over CDN)  
- [ ] Hardened eBPF probes (beyond `/proc/net/tcp` fallback)  
- [ ] K8s operator (beyond Helm chart)  
- [ ] Hosted plugin registry  
- [ ] Production Tauri builds with WebGL polish  
